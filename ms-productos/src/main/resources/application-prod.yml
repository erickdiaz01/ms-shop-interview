spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${JWT_ISSUER_URI}   # requerido en prod
logging:
  config: classpath:logback-spring.xml
  level:
    com.company: WARN
management:
  tracing:
    sampling:
      probability: 0.05
